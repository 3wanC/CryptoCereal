# CryptoCereal
This is a private encryption software.
It uses a brutalist approach by using a serial key the same length of the encrypted information.
This means the encryption is indestructible, but can be described as a "glass cannon" as it is extremely fragile if the key is not kept safe.

## Development Stage
Very much in early development. Do not expect any functionality as off now.

## Todo List
* Update Encryption.py to use secrets import instead of random
* Create simple set up allowing editing of information on pyhton console
* Create a python text editing application as part of distribution package
* Create a dual key offline system to allow secure corraspondance
* Create other python file editors, for example spreadsheets
